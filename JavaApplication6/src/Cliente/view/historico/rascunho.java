/*
 * To change this license header, choose License Headers in Project Properties.
 * To change this template file, choose Tools | Templates
 * and open the template in the editor.
 */
package Cliente.view.historico;

import Cliente.Cliente;
import Cliente.view.BatePapo;
import Cliente.view.TelaCliente;
import java.io.IOException;
import java.util.logging.Level;
import java.util.logging.Logger;
import javax.swing.JOptionPane;

/**
 *
 * @author Adston at self
 */
public abstract class rascunho {
 /*
      if( this.cliente == null ){
      this.cliente = new Cliente( this.campoDestino.getText(), Integer.valueOf( this.campoPorta.getText() ) );
         if( this.cliente.conectaServidor() ){
            
            if( validaNick() ){
               this.cliente.setNick( this.nick );
               this.cliente.setTela( this );
               this.cliente.iniciaCliente();
               
               this.h.getLer().getEscrita().IP = this.campoDestino.getText();
               try {
                  this.h.getLer().getEscrita().addEnd();
               } catch (IOException ex) {
                  Logger.getLogger(TelaCliente.class.getName()).log(Level.SEVERE, null, ex);
               }
               
                  bp = new BatePapo();
                  bp.setConectado(this.cliente);
                  bp.setTitle(this.cliente.getNick());
                  bp.setVisible(true);
                  this.dispose();
            }else{
               JOptionPane.showMessageDialog( this, "Login Não realizado" );
               try {
                  this.cliente.finaliza();
                  this.cliente = null;
               } catch (IOException ex) {
                  Logger.getLogger(TelaCliente.class.getName()).log(Level.SEVERE, null, ex);
               }
            }
         }else{
            JOptionPane.showMessageDialog( this, "Não foi possivel encontrar o servidor", 
                    "Erro ao conectar", 0);
            this.cliente = null;
         }
   }
   */
   
   
   
   
   
   
   
   
   
}
